{
  "buildCommand": "npx flutter_bash install && flutter build web --release --base-href '/'",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "installCommand": "npm install --quiet"
}
